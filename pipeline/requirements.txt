# Terra Nexus pipeline — Python 依存
# 注: pysheds 0.5 は numpy 2.x で削除された np.in1d を使うため
#     terranexus.compat が np.isin で補う（numpy のピン留めは不要）。
numpy>=1.26
rasterio>=1.3
geopandas>=0.14
shapely>=2.0
pysheds>=0.4
pyproj>=3.5
scipy>=1.10          # 生息地質の距離減衰（畳み込み）に使用
pyyaml>=6.0

# タイル生成（任意）
rio-rgbify>=1.0        # DEM -> terrain-RGB（地形 PMTiles）
pmtiles>=3.2           # pmtiles-convert（MBTiles -> PMTiles）
# ベクタ PMTiles は tippecanoe（別途 PATH に導入）を使用

# 開発用
pytest>=7.0
